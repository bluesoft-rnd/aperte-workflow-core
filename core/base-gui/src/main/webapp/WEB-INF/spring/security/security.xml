<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">
	


	<!-- APPLICATION ACCESS CONFIG -->
	<http auto-config="false" use-expressions="true" entry-point-ref="loginEntryPoint">
	
		<!-- STATIC RESOURCES -->
		<intercept-url pattern="/css/**" access="permitAll"  requires-channel="http"/>
		<intercept-url pattern="/images/**" access="permitAll"  requires-channel="http"/>
		<intercept-url pattern="/js/**" access="permitAll"  requires-channel="http"/>

		<!-- MAIN PAGE, LOGIN PAGE -->
		<intercept-url pattern="/" access="permitAll"  requires-channel="http" />
		<intercept-url pattern="/main.do" access="permitAll"  requires-channel="http" />
		<intercept-url pattern="/login" access="permitAll"  requires-channel="http" />

		<!-- MAIN RULE - ONLY AUTHENTICATED ALLOWED -->
		<intercept-url pattern="/**" access="isAuthenticated()" />
		
		<logout logout-url="/logout" invalidate-session="true"/>
	</http>
	
	<!-- CLIENT LOGIN ENTRY POINT -->
	<beans:bean id="loginEntryPoint" class="org.springframework.security.web.authentication.LoginUrlAuthenticationEntryPoint">
		<beans:constructor-arg value="/login" />
	</beans:bean>
	
</beans:beans>